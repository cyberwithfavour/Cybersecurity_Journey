# Day 20 Tasks

- [ ] Use `ping google.com` and observe the response.
- [ ] Run `ip a` to view your network interfaces and IP address.
- [ ] Run `hostname -I` to display your IP address.
- [ ] Use `nslookup google.com` to resolve a domain name.
- [ ] Run `ss -tuln` to view active listening ports.
- [ ] Install Wireshark (if not already installed) and explore its interface.
- [ ] Explain when you would use:
  - ping
  - traceroute
  - nslookup
  - ip
  - ss
